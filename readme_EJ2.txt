1) El tibble muestra solo 10 filas, el tipo de cada columna (<dbl>, <chr>) y no se desborda de la pantalla. Por otro lado, el data.frame imprime las 559 filas completas y no indica los tipos de dato en el encabezado. El contenido es el mismo, cambia la forma de presentarlo.

3) Los perfiles son bastante similares entre varones y mujeres, sin una diferencia marcada en la forma de la distribución.

Una aclaración importante: en el inciso 2, interpretamos "frecuencia relativa por sexo" como el % que cada sexo representa dentro del grupo que contestó que sí (no el % de sí dentro de cada sexo)
